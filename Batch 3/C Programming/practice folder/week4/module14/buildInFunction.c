#include<stdio.h>
#include<math.h>
#include<stdlib.h>
    
int main()
{
    // double a, b;
    // scanf("%lf %lf", &a, &b);
    // double ans = pow(a,b);
    // printf("%0.2lf", ans);

    // int a, b;
    // scanf("%d %d", &a, &b);
    // int ans = pow(a*1.0,b*1.0);
    // printf("%d", ans);

    int a;
    scanf("%d", &a);
    int ans = abs(a);
    printf("%d", ans);

    return 0;
}